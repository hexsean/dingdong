services:
  dingdong:
    image: hexsean/dingdong:${DINGDONG_VERSION:-latest}
    pull_policy: always
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    tty: true
    stdin_open: true
